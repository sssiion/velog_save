<pre><code>1016

$('body').css('background', ''); // ''는 비어있으므로 투명색이 나옴.

버튼에 클릭 이벤트를 건다, 버튼이 클릭되면 함수가 실행된다.
$('button').click(changeBodyBackground);
! 함수를 넣을 때 ()없이 함수 이름만 넣기.

max-width: 1000px; 글자를 

팝업 버튼 사용
display: none -&gt; block 변경

#('.class').click(function() {
    $( ' ').css('display', 'block');
});

$('.class').hover(function);
hover 도 되고
mouseenter, mouseleave 도 가능함 (마우스 있을 때, 떠났을 때)
</code></pre>